<Application x:Class="AtomicDriftTuner.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <!-- General application colors -->
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0E1116"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#171B22"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#20252D"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#242931"/>
        <SolidColorBrush x:Key="InputBrush" Color="#272C34"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3A414D"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#9FA8B8"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#AEB6C3"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00CFE8"/>
        <SolidColorBrush x:Key="AccentTextBrush" Color="#071014"/>

        <!-- Explicit input-field colors -->
        <SolidColorBrush x:Key="InputTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#3A414D"/>

        <!-- Explicit DataGrid/table colors -->
        <SolidColorBrush x:Key="DataGridBackgroundBrush" Color="#20252D"/>
        <SolidColorBrush x:Key="DataGridAlternateBackgroundBrush" Color="#242931"/>
        <SolidColorBrush x:Key="DataGridTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="DataGridHeaderBackgroundBrush" Color="#171B22"/>
        <SolidColorBrush x:Key="DataGridHeaderTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="DataGridSelectedBackgroundBrush" Color="#00CFE8"/>
        <SolidColorBrush x:Key="DataGridSelectedTextBrush" Color="#071014"/>
        <SolidColorBrush x:Key="DataGridGridLineBrush" Color="#3A414D"/>

        <!-- Explicit TabControl/TabItem colors -->
        <SolidColorBrush x:Key="TabHeaderBackgroundBrush" Color="#20252D"/>
        <SolidColorBrush x:Key="TabHeaderTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="TabSelectedBackgroundBrush" Color="#00CFE8"/>
        <SolidColorBrush x:Key="TabSelectedTextBrush" Color="#071014"/>
        <SolidColorBrush x:Key="TabBorderBrush" Color="#3A414D"/>

        <!-- Explicit CheckBox colors -->
        <SolidColorBrush x:Key="CheckBoxTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="CheckBoxBackgroundBrush" Color="#272C34"/>
        <SolidColorBrush x:Key="CheckBoxBorderBrush" Color="#3A414D"/>
        <SolidColorBrush x:Key="CheckBoxCheckMarkBrush" Color="#00CFE8"/>

        <!-- ComboBox / dropdown colors -->
        <SolidColorBrush x:Key="ComboBoxBackgroundBrush" Color="#272C34"/>
        <SolidColorBrush x:Key="ComboBoxTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="ComboBoxDropDownBackgroundBrush" Color="#20252D"/>
        <SolidColorBrush x:Key="ComboBoxDropDownTextBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="ComboBoxHighlightBrush" Color="#00CFE8"/>
        <SolidColorBrush x:Key="ComboBoxHighlightTextBrush" Color="#071014"/>
        <SolidColorBrush x:Key="ComboBoxBorderBrush" Color="#3A414D"/>

        <!-- TextBox: force field background/text/border instead of OS defaults. -->
        <Style x:Key="AtomicTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource InputTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource InputTextBrush}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="SelectionTextBrush" Value="{DynamicResource AccentTextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource AtomicTextBoxStyle}"/>

        <!-- CheckBox: explicit text, box, border and check glyph colors. -->
        <Style x:Key="AtomicCheckBoxStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{DynamicResource CheckBoxTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource CheckBoxBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CheckBoxBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="CheckBoxBorder"
                                    Grid.Column="0"
                                    Width="18"
                                    Height="18"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <Path x:Name="CheckMark"
                                          Visibility="Collapsed"
                                          Width="11"
                                          Height="8"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Stroke="{DynamicResource CheckBoxCheckMarkBrush}"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Data="M 1 4 L 4 7 L 10 1"/>
                                    <Rectangle x:Name="IndeterminateMark"
                                               Visibility="Collapsed"
                                               Width="10"
                                               Height="2"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Fill="{DynamicResource CheckBoxCheckMarkBrush}"/>
                                </Grid>
                            </Border>

                            <ContentPresenter Grid.Column="1"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="CheckBoxBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource AtomicCheckBoxStyle}"/>

        <!-- ComboBox -->
        <Style x:Key="AtomicComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{DynamicResource ComboBoxDropDownTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ComboBoxDropDownBackgroundBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboBoxHighlightBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ComboBoxHighlightTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboBoxHighlightBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ComboBoxHighlightTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AtomicComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="{DynamicResource ComboBoxTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ComboBoxBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ComboBoxBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="MaxDropDownHeight" Value="340"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource AtomicComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
                                    <Border Grid.Column="1"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1,0,0,0">
                                        <Path Width="9"
                                              Height="5"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Fill="{DynamicResource ComboBoxTextBrush}"
                                              Data="M 0 0 L 4.5 5 L 9 0 Z"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <ToggleButton Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Opacity="0.01"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Margin="0,2,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Background="{DynamicResource ComboBoxDropDownBackgroundBrush}"
                                        BorderBrush="{DynamicResource ComboBoxBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource AtomicComboBoxItemStyle}"/>
        <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource AtomicComboBoxStyle}"/>

        <!-- DataGrid: explicit templates prevent platform theme white-on-white. -->
        <Style x:Key="AtomicDataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource DataGridHeaderTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,7"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HeaderBorder"
                                        Property="Background"
                                        Value="{DynamicResource PanelAltBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AtomicDataGridCellStyle" TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource DataGridTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DataGridSelectedBackgroundBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource DataGridSelectedTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AtomicDataGridRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="{DynamicResource DataGridBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource DataGridTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="{DynamicResource DataGridAlternateBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DataGridSelectedBackgroundBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource DataGridSelectedTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AtomicDataGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="{DynamicResource DataGridBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource DataGridTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="RowBackground" Value="{DynamicResource DataGridBackgroundBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGridAlternateBackgroundBrush}"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource AtomicDataGridColumnHeaderStyle}"/>
            <Setter Property="CellStyle" Value="{StaticResource AtomicDataGridCellStyle}"/>
            <Setter Property="RowStyle" Value="{StaticResource AtomicDataGridRowStyle}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
        <Style TargetType="{x:Type DataGrid}" BasedOn="{StaticResource AtomicDataGridStyle}"/>

        <!-- Tab headers/content: explicit theme instead of OS defaults. -->
        <Style x:Key="AtomicTabItemStyle" TargetType="{x:Type TabItem}">
            <Setter Property="Background" Value="{DynamicResource TabHeaderBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TabHeaderTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource TabBorderBrush}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5,5,0,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="HeaderBorder"
                                        Property="Background"
                                        Value="{DynamicResource TabSelectedBackgroundBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource TabSelectedTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HeaderBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AtomicTabControlStyle" TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource TabBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource AtomicTabItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0"
                                      IsItemsHost="True"
                                      KeyboardNavigation.TabIndex="1"
                                      Panel.ZIndex="1"
                                      Background="Transparent"/>
                            <Border Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="1">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  Margin="0"
                                                  ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource AtomicTabItemStyle}"/>
        <Style TargetType="{x:Type TabControl}" BasedOn="{StaticResource AtomicTabControlStyle}"/>
    </Application.Resources>
</Application>
