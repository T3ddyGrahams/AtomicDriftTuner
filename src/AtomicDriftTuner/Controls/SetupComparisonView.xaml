<UserControl x:Class="AtomicDriftTuner.Controls.SetupComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AtomicDriftTuner.Controls">
    <UserControl.Resources>
        <Style TargetType="TextBlock"><Setter Property="TextWrapping" Value="Wrap"/><Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/></Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="Your setup changes" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource SectionHeadingBrush}" Margin="0,8,0,6"/>
        <TextBlock x:Name="ContextText" Text="Choose a setup or two saved runs to compare." Margin="0,0,0,6"/>
        <CheckBox x:Name="ShowAllBox" Content="Show all settings (including unchanged)" Checked="Filter_Changed" Unchecked="Filter_Changed" AutomationProperties.Name="Show all setup settings including unchanged" Margin="0,4,0,6"/>
        <TextBlock x:Name="CountText" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,6"/>
        <ScrollViewer x:Name="RowsScroll" MaxHeight="380" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Focusable="True" AutomationProperties.Name="Setup values comparison">
            <ItemsControl x:Name="RowsList" HorizontalContentAlignment="Stretch">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource PanelBrush}" Padding="10" Margin="0,0,4,8">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers><DataTrigger Binding="{Binding Emphasized}" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter Property="BorderThickness" Value="3,1,1,1"/></DataTrigger></Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock Text="{Binding Setting}" TextWrapping="Wrap" FontWeight="Bold" FontSize="15"/>
                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,6"><Run Text="{Binding Category}"/><Run Text=" · "/><Run Text="{Binding Status}"/></TextBlock>
                                <local:AdaptivePanel MinItemWidth="135" MaxColumns="3" Spacing="8">
                                    <StackPanel><TextBlock Text="{Binding BeforeHeading}" TextWrapping="Wrap" Foreground="{DynamicResource FieldLabelBrush}"/><TextBlock Text="{Binding Before}" TextWrapping="Wrap" FontWeight="Bold"/></StackPanel>
                                    <StackPanel><TextBlock Text="{Binding AfterHeading}" TextWrapping="Wrap" Foreground="{DynamicResource FieldLabelBrush}"/><TextBlock Text="{Binding After}" TextWrapping="Wrap" FontWeight="Bold"/></StackPanel>
                                    <StackPanel><TextBlock Text="Difference" TextWrapping="Wrap" Foreground="{DynamicResource FieldLabelBrush}"/><TextBlock Text="{Binding Difference}" TextWrapping="Wrap" FontWeight="Bold"/></StackPanel>
                                </local:AdaptivePanel>
                                <TextBlock Text="{Binding Explanation}" TextWrapping="Wrap" Margin="0,8,0,4"/>
                                <Expander Header="Saved values and source" HorizontalContentAlignment="Stretch"><TextBlock Text="{Binding Details}" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4"/></Expander>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</UserControl>
