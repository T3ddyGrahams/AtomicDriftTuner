<UserControl x:Class="AtomicDriftTuner.Controls.TrackSectionView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:AtomicDriftTuner.Controls">
 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
  <StackPanel Margin="12">
   <TextBlock Text="TRACK &amp; SECTIONS · Section goals" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
   <TextBlock Margin="0,6" TextWrapping="Wrap" Text="Mark a section of your recorded route, save a goal, then compare complete passes. These goals are separate from global Desired Behavior. Goals selected after recording are retrospective review targets."/>
   <TextBlock x:Name="CaptureText" Text="Select a saved run. Old recordings have no world positions." TextWrapping="Wrap" Margin="0,6"/>
   <local:TrackMap x:Name="Map" Height="330" Margin="0,8"/>
   <TextBlock TextWrapping="Wrap" Text="X/Z map · gray: driven path · faint gray: optional AI context · gold: reference (circle start, square end) · green: offset aim · blue: before · pink: after · white: cursor. Gaps stay disconnected. Click near the path or move the time slider."/>
   <Slider x:Name="TimeSlider" Minimum="0" Maximum="1" ValueChanged="CursorChanged" Margin="0,10"/>
   <WrapPanel>
    <TextBlock x:Name="CursorText" Width="140" VerticalAlignment="Center"/>
    <Button x:Name="MarkStartButton" Content="Mark start" Click="MarkStart" Margin="0,0,8,8" Padding="10,6"/>
    <Button x:Name="MarkEndButton" Content="Mark end" Click="MarkEnd" Margin="0,0,8,8" Padding="10,6"/>
    <TextBlock x:Name="RangeText" VerticalAlignment="Center"/>
   </WrapPanel>
   <TextBlock Text="Section name"/>
   <TextBox x:Name="NameBox" MaxLength="80" Margin="0,4,0,8"/>
   <WrapPanel>
    <StackPanel Width="210" Margin="0,0,12,8">
     <TextBlock Text="Body-angle range ° (optional)"/>
     <StackPanel Orientation="Horizontal"><TextBox x:Name="AngleMinBox" Width="85" Margin="0,4,8,0"/><TextBox x:Name="AngleMaxBox" Width="85" Margin="0,4,0,0"/></StackPanel>
    </StackPanel>
    <StackPanel Width="210" Margin="0,0,12,8">
     <TextBlock Text="Preferred forward gear (optional)"/>
     <TextBox x:Name="GearBox" Margin="0,4,0,0"/>
    </StackPanel>
   </WrapPanel>
   <WrapPanel>
    <StackPanel Width="210" Margin="0,0,12,8"><TextBlock Text="Reference line aim"/><ComboBox x:Name="LineAimBox" Margin="0,4,0,0"/></StackPanel>
    <StackPanel Width="210" Margin="0,0,12,8"><TextBlock Text="Offset metres (0 for reference)"/><TextBox x:Name="OffsetBox" Text="0" Margin="0,4,0,0"/></StackPanel>
   </WrapPanel>
   <TextBlock TextWrapping="Wrap" Text="For a wider-line aim, choose a signed map offset and inspect its green aim after saving. Positive is to the left of travel as drawn on this map. ADT does not infer which side is outside a corner or whether it is safe. Route offset measures position; body angle measures car orientation. Road geometry and usable width remain unknown." Margin="0,2,0,8"/>
   <Button x:Name="SaveSectionButton" Content="Save section and goal" Click="SaveSection" HorizontalAlignment="Left" Padding="12,8"/>
   <TextBlock Text="Saved sections · each save creates a new revision" Margin="0,16,0,4"/>
   <ComboBox x:Name="SectionBox" SelectionChanged="SectionChanged"/>
   <TextBlock x:Name="GoalText" TextWrapping="Wrap" Margin="0,8"/>
   <TextBlock x:Name="PassSummary" TextWrapping="Wrap" Margin="0,8"/>
   <TextBlock x:Name="BeforeLabel" Text="Before pass (selected baseline, or same run)"/>
   <ComboBox x:Name="BeforeBox" SelectionChanged="PassChanged" Margin="0,4,0,8"/>
   <TextBlock Text="After pass (selected run)"/>
   <ComboBox x:Name="AfterBox" SelectionChanged="PassChanged" Margin="0,4,0,8"/>
   <TextBlock x:Name="Findings" TextWrapping="Wrap" Margin="0,8"/>
   <local:GoalFeedbackView x:Name="SectionFeedback"/>
   <TextBlock Text="Saved reviews for these passes" TextWrapping="Wrap" Margin="0,8,0,4"/>
   <ComboBox x:Name="SectionReviewHistory" SelectionChanged="ReviewSelected" AutomationProperties.Name="Section review history"/>
   <TextBlock x:Name="SectionReviewText" TextWrapping="Wrap" Margin="0,6"/>
   <TextBlock x:Name="Status" TextWrapping="Wrap" Margin="0,10" Foreground="{DynamicResource AccentBrush}"/>
  </StackPanel>
 </ScrollViewer>
</UserControl>
